spring:
  datasource:
    url: jdbc:mysql://localhost:3306/commerce
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
springdoc:
  swagger-ui:
    path: /swagger-ui.html

jwt:
  key: secret_key
  user_expires_minute: 15
  admin_expires_minute: 2400
  seller_expires_minute: 1200
